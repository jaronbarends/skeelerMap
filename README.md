# SkateMap

## Setup

`MapLeafletContainer` takes care of all map-related functionality. It exposes methods to `MapUIContainer`'s `mapRef` with `useImperativeHandle`. Other Map UI elements use those exposed methods.
